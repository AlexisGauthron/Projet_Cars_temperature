# =============================================================================
# Requirements pour le Benchmark de Détection de Visages
# =============================================================================
# Installation: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# Core - Requis pour tous les modèles
# -----------------------------------------------------------------------------
numpy>=1.21.0
opencv-python>=4.8.0
scipy>=1.9.0                    # Pour charger les annotations MAFA (.mat)
tqdm>=4.65.0                    # Barres de progression
Pillow>=9.0.0                   # Manipulation d'images

# -----------------------------------------------------------------------------
# Modèles OpenCV natifs (YuNet, Haar, OpenCV-DNN)
# -----------------------------------------------------------------------------
# Inclus dans opencv-python, aucune dépendance supplémentaire

# -----------------------------------------------------------------------------
# MediaPipe Face Detection
# -----------------------------------------------------------------------------
mediapipe>=0.10.0

# -----------------------------------------------------------------------------
# MTCNN (Multi-task Cascaded CNN)
# -----------------------------------------------------------------------------
mtcnn>=0.1.1                    # Version CPU simple
facenet-pytorch>=2.5.0          # Version PyTorch (meilleure précision)
torch>=2.0.0                    # Requis pour facenet-pytorch

# -----------------------------------------------------------------------------
# RetinaFace & SCRFD (InsightFace)
# -----------------------------------------------------------------------------
insightface>=0.7.3
onnxruntime>=1.15.0             # Backend CPU pour InsightFace

# Pour GPU (optionnel, remplacer onnxruntime par):
# onnxruntime-gpu>=1.15.0

# -----------------------------------------------------------------------------
# DLib (HOG & CNN)
# -----------------------------------------------------------------------------
dlib>=19.24.0
# Note: Sur macOS, peut nécessiter: brew install cmake

# -----------------------------------------------------------------------------
# YOLO Face Models (v5, v8, v9, v10, v11, v12)
# -----------------------------------------------------------------------------
ultralytics>=8.1.0

# -----------------------------------------------------------------------------
# Modèles ONNX (FaceBoxes, CenterFace, DSFD, TinaFace)
# -----------------------------------------------------------------------------
# onnxruntime déjà inclus ci-dessus

# -----------------------------------------------------------------------------
# Optionnel - Accélération GPU
# -----------------------------------------------------------------------------
# torch-cuda>=2.0.0             # Pour GPU NVIDIA (MTCNN PyTorch)
# onnxruntime-gpu>=1.15.0       # Pour GPU (remplace onnxruntime)

# -----------------------------------------------------------------------------
# Optionnel - Téléchargement datasets
# -----------------------------------------------------------------------------
gdown>=4.7.0                    # Téléchargement depuis Google Drive
requests>=2.28.0
